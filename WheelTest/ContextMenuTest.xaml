<Window x:Class="WheelTest.ContextMenuTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WheelTest"
        mc:Ignorable="d"
        Title="ContextMenuTest"
        Height="450"
        Width="800">
    <Grid Height="400">
        <TextBlock  Text="dsadasdasdasd" />
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem x:Name="warehouseCarrierServiceMenuItem"
                          Header="指定线上发货物流(仅勾选)">
                    
                    <MenuItem.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Path=ChannelName}" />
                                <TextBlock Foreground="#999999"
                                           Text="{Binding Path=DeliveryAddress1}"
                                           x:Name="deliveryAddress" />
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Path=DeliveryAddress1}"
                                             Value="{x:Null}">
                                    <Setter TargetName="deliveryAddress"
                                            Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </MenuItem.ItemTemplate>
                </MenuItem>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>
