<Window x:Class="WheelTest.VirtualizingWrapPanelTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WheelTest"
        mc:Ignorable="d"
        Title="虚拟化测试"
        Height="450"
        Width="800">
    <Window.Resources>
        <Style x:Key="DefaultMultiSelectListBoxItemStyle"
               TargetType="ListBoxItem">
            <Setter Property="OverridesDefaultStyle"
                    Value="True" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="0,0,20,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <CheckBox Content="{TemplateBinding Content}"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                  IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSelected, Mode=TwoWay}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="{x:Type local:MultiSelectListBox}"
               TargetType="{x:Type local:MultiSelectListBox}">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderBrush"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Padding"
                    Value="1" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <local:VirtualizingWrapPanel  ScrollOffset="50"
                                                      ChildHeight="70"
                                                      ChildWidth="70" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle"
                    Value="{StaticResource DefaultMultiSelectListBoxItemStyle}" />
        </Style>
    </Window.Resources>
    <Grid>
        <local:MultiSelectListBox Margin="0,3"
                                  Grid.Row="1"
                                  Height="150"
                                  Grid.Column="1"
                                  x:Name="_values"
                                  VerticalAlignment="Center">
            <local:MultiSelectListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="OverridesDefaultStyle"
                            Value="True" />
                    <Setter Property="Height"
                            Value="21" />
                    <Setter Property="Width"
                            Value="90" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <CheckBox VerticalAlignment="Center"
                                          IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSelected, Mode=TwoWay}">
                                    <AccessText Text="{Binding }"
                                                TextTrimming="CharacterEllipsis"></AccessText>
                                </CheckBox>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </local:MultiSelectListBox.ItemContainerStyle>
        </local:MultiSelectListBox>
    </Grid>
</Window>
